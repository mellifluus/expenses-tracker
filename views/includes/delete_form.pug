form(action='/settings/deleteUser', method='post', autocomplete='off') 
  label(for='confirmation') Confirm account deletion
  br
  br
  input(
    type='text',
    name='confirmation',
    placeholder='DELETE',
    style='background-color: #ffbaba'
  ) 
  if messages
    if messages.confirmation
      p#delete-form-error #{ messages.confirmation }
  input(
    type='submit',
    value='Delete',
    style='background-color: #ffbaba; color: #d8000c'
  )
