form(action='/settings/deleteUser', method='post', autocomplete='off') 
  label(for='confirmation', style='color: red') Confirm account deletion
  br
  br
  input(type='text', name='confirmation', placeholder='DELETE') 
  if messages
    if messages.confirmation
      p#delete-form-error #{ messages.confirmation }
  input(type='submit', value='Delete')
